# models/marts/fct_orders.yml
version: 2

models:
  - name: fct_orders
    columns:
      - name: customer_id
        tests:
          - not_null
      - name: order_date
        tests:
          - not_null
