version: 2

models:
  - name: fct_payments
    columns:
      - name: customer_id
        tests:
          - not_null
      - name: payment_method
        tests:
          - not_null
      - name: total_paid
        tests:
          - not_null
